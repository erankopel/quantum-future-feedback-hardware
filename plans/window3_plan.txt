================================================================
PAPER VII, WINDOW 3: PRE-REGISTERED PLAN
Pre-registered 25 Aug 2026, before the window opens. ASCII, no em
dashes, series discipline. Ordering fixed by the author: virtual-Z
test first, deeper gauge-fixed two-qubit GST second (conditional),
stability study last.
================================================================

WINDOW. The rolling 28-day cap frees the 594 s spent on 24 Aug 2026
between 21 Sep 16:03 and 23:10 UTC; from ~19:10 IDT on 21 Sep the
account is effectively a fresh 600 s. The 6 s available before then
are below the smallest useful job and are ignored. All costs below
use the charge-fitted model cost = N * (0.0121 + 2.577e-4 * shots),
validated to within 2 per cent on five actual charges; the first job
of the window recalibrates it.

W3-0  PREFLIGHT (0 s). Confirm usage 600 s; ibm_kingston operational;
      triple (21,22,36) alive in the current calibration with both
      links present (a dead link STOPS the plan for re-planning, the
      series comparability depends on the triple); GCORE re-gate of
      the analysis pipeline. Record the calibration snapshot.

W3-1  VIRTUAL-Z FIVE-ARM SCAN (rung11b, 360 circuits x 1024 shots,
      ~99 s). rz(k * 2.84 deg), k in {0,-1,+1,-2,+2}, on the message
      qubit after each of the 15 (21,36) CZ gates, one atomic job,
      GVZ gate (rz-only diffs, 15 insertions per corrected arm,
      equal-opposite pairs, rz virtual so schedules identical).
      Pre-registered predictions:
        V1 the noise-map rotation angle is V-shaped in k;
        V2 the minimum is displaced to negative k, below the plain
           arm by >= 3 sigma;
        V3 eta falls at the minimum while eta' is flat across arms;
        V4 at the minimum the isotropic integer equals the measured
           integer.
      GATE G-VZ: V1+V2 hold -> Path A (W3-2 skipped, the correction
      is validated functionally). Flat or ambiguous -> Path B. Both
      signed arms moving the same direction -> drift artefact: one
      3-arm repeat (60 s from reserve) before deciding.

W3-2  DEEPER GAUGE-FIXED 2Q GST, PATH B ONLY (rung10 script,
      --pair 21,36 --maxL 8 --shots 384: 1813 FPR circuits, ~201 s,
      GGST2 dry-run PASSED 25 Aug). Doubling L roughly halves the
      eigenphase uncertainty (1.5 -> ~0.8 deg). Justification
      sharpened by prep finding P2 (below): per-component
      attribution cannot be rescued from the existing data by gauge
      fixing, so if the scan does not validate the correction, the
      deeper dataset is the only route to the mechanism.

W3-3  STABILITY STUDY, LAST (rung8 upgraded). Epochs alternate
      plain-only (72 circuits, ~20 s) and plain+corrected (144,
      ~40 s), the corrected arm using theta_opt from the W3-1 V-fit
      (w3_theta.json), so the study measures the drift of eta, the
      rotation, the index, AND whether theta_opt itself is
      epoch-stable. Every epoch logs the full explorer bundle plus
      the calibration snapshot. Cadence: four epochs on day one at
      ~3 h spacing, then one per day.
        Path A: 12 epochs (~360 s), span ~9 days, reserve ~141 s.
        Path B: 7 epochs (~200 s), span ~4 days, reserve ~100 s.
      GATE G-STAB: no submission that projects total consumption
      past 550 s (enforced in code); epochs stop when the remaining
      budget is below 50 s.

LEDGER SUMMARY (fresh 600 s):
      Path A: 99 (W3-1) + 360 (W3-3) = 459 s, reserve 141 s.
      Path B: 99 + 201 (W3-2) + 200 (W3-3) = 500 s, reserve 100 s.

PREP RECORD (built, dry-run and committed 25 Aug 2026, zero QPU):
  P1  rung11b_vzscan.py: GVZ PASS on live backend (5 arms, 15
      insertions per corrected arm circuit, rz virtual on all three
      qubits); budget guard verified: refused at 594 s consumed.
  P2  gauge_fixed_components.py: gauge fixing that pins the
      single-qubit gates (item weight 1e3 vs 1e-3 on the CZ)
      applied to both fitted models. FINDING, negative and
      recorded: under deliberate gauge scrambling the re-fixed CZ
      components deviate by 0.8 to 1.3 deg on both links, the same
      scale as the bootstrap non-centring, so the fixing does NOT
      rescue per-component attribution at current depth. The
      per-component numbers remain gauge-sensitive indications;
      only the invariant eigenphases are quotable. This is the
      standing justification for W3-2 on Path B.
  P3  FPR L<=8 design built and GGST2-gated: 1813/1813 circuits
      preserve sx and cz counts; 201 s at 384 shots.
  P4  rung8_epoch.py upgraded: corrected arm verified (+15 rz, all
      other op counts identical), theta plumbing from w3_theta.json,
      alternation by epoch parity, CAP 550 s guard in the submit
      path.

RISKS. R1 theta staleness across the month: absorbed by design, the
scan covers 0 to 5.7 deg in both signs. R2 triple health unknown on
21 Sep: W3-0 gates it. R3 queue depth swings (2 to 277 observed):
wall-clock only, execution-second billing makes waiting free.
R4 sign conventions: the signed arms make the sign an output of the
experiment, not an input assumption.
================================================================
